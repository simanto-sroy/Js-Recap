// Operator 

// Arithmetic Operator
    // +, -, *, /, %, ++, --

// Assignment Operator
    //=, +=, -=, *=, %=, /=

// Comparison Operator
    // ==, === !=, !== 

// Logical Operator
    // &&, ||, !

// Bitwise operator
    // <, <=, >=, >, <<, >>